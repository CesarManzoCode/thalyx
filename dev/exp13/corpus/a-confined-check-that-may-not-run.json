{
  "about": "A real launch through the confinement a program nobody signed gets. Where nothing in the kernel enforces, it is not_proven and never a pass.",
  "tree": {
    "Cargo.toml": "[workspace]\n\n[package]\nname = \"exp13\"\nversion = \"0.1.0\"\nedition = \"2021\"\n",
    "src/lib.rs": "pub mod boot;\npub mod keystore;\n",
    "src/keystore.rs": "pub struct Keystore;\n\npub fn unlock() -> Keystore {\n    Keystore\n}\n",
    "src/boot.rs": "use crate::keystore::Keystore as Keys;\n\npub fn boot() -> Keys {\n    crate::keystore::unlock()\n}\n"
  },
  "agent": [
    {
      "verb": "exec",
      "program": {
        "label": "confined",
        "steps": [
          {
            "verb": "edit",
            "arguments": [
              "src/keystore.rs",
              "sustituir",
              "pub fn unlock",
              "pub fn open"
            ]
          }
        ],
        "validate": [
          {
            "check": "program",
            "program": "/usr/bin/true"
          }
        ]
      }
    }
  ],
  "depends_on": [
    "kernel policy map loaded",
    "confined launch"
  ]
}
