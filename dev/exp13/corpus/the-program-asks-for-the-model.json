{
  "about": "A program that decides the next decision is not a machine's: it changed something first, and needs_model is not a success.",
  "tree": {
    "Cargo.toml": "[workspace]\n\n[package]\nname = \"exp13\"\nversion = \"0.1.0\"\nedition = \"2021\"\n",
    "src/lib.rs": "pub mod boot;\npub mod keystore;\n",
    "src/keystore.rs": "pub struct Keystore;\n\npub fn unlock() -> Keystore {\n    Keystore\n}\n",
    "src/boot.rs": "use crate::keystore::Keystore as Keys;\n\npub fn boot() -> Keys {\n    crate::keystore::unlock()\n}\n"
  },
  "agent": [
    {
      "verb": "exec",
      "program": {
        "label": "needs model",
        "run": "const answer = thalyx.context(\"Keystore\");\nthalyx.mustWork(thalyx.substitute(\"src/keystore.rs\", \"pub fn unlock\", \"pub fn maybe\"), \"edit\");\nreturn thalyx.needModel({ question: \"is this the one\", candidates: answer.entries.length });\n"
      }
    }
  ]
}
