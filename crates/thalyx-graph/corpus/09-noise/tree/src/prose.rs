/*
 * The beacon is described here, in a block comment, and this is not a use.
 */

pub fn announce() {
    // beacon is named in a line comment, which is not a use either.
    let message = "beacon appears in this string and that is not a use";
    let spanning = "a string that runs over \
                    two lines mentioning beacon";
    let _ = (message, spanning);
}
